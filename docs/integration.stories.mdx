import { Meta } from "@storybook/addon-docs";
import { ArchiveLink } from "../src/docs/ArchiveLink";

<Meta title="Docs/Integration" />

# Integration

You can download an archive of the static assets <ArchiveLink>here</ArchiveLink>.

## CSS

```html
<link rel="stylesheet" href="index.css" />
```

❗ Make sure the `fonts` folder sits next to the `index.css`.

## Javascript

```html
<script type="module" src="index.js"></script>
```

## Icon sprite

Add the content of `icon-sprite.html` to your HTML document.
